# Python backend deps for LLM Fusion Studio
mergekit>=0.0.5
huggingface_hub>=0.23.0
torch>=2.2.0  # mergekit depends on torch
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
psutil>=5.9.0
# llama.cpp is built separately; the backend calls its `quantize` binary via subprocess.
